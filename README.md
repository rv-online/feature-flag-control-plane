# Feature Flag Control Plane

TypeScript service-style project for feature rollout governance. It focuses on state transitions, operational rules, and deterministic tests rather than UI polish.

## What It Shows

- config-driven releases, ownership, and safe rollout mechanics
- typed domain logic separated from transport
- Node test coverage and production-style scripts

## Scripts

```bash
npm test
npm run build
```
